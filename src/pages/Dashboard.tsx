export default function Dashboard(_props: { setCurrentPage?: (page: string) => void }) {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <p className="text-zinc-500 text-2xl font-bold">추후 개선 예정입니다.</p>
    </div>
  );
}
